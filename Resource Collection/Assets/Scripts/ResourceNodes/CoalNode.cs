using UnityEngine;
using System.Collections;

public class CoalNode : BasicResourceNode
{

    // Use this for initialization
    void Start()
    {
        itemLocation = 3;
        worth = 1;
        setUp();
    }

    // Update is called once per frame
    void Update()
    {

    }
}
